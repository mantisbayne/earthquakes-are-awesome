<resources>

    <!-- Base application theme. -->
    <style name="Material.Brown" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:colorPrimary">@color/primary_material_light</item>
        <item name="android:colorPrimaryDark">@color/primary_dark_material_light</item>
        <item name="android:colorAccent">@android:color/primary_text_light</item>
        <item name="android:navigationBarColor">@color/primary_dark_material_light</item>
    </style>

    <style name="Material.Brown.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

</resources>
